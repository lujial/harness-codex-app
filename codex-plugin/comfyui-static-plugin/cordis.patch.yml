# comfyui-static bundle patch: inserts its public dsh.client entry into the web plugin roster.
# The bundle is the official dsh plugin shape — install it with
# `dsh plugin --profile web add <path-or-git-url>`.
- insert:
    - id: comfyui-static
      name: '@dsh-external/dsh-plugin-comfyui'
