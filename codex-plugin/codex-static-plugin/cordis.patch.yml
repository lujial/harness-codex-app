# codex-static bundle patch: inserts its public dsh.client entry into the web plugin roster.
# The bundle is the official dsh plugin shape — install it with
# `dsh plugin --profile web add <path-or-git-url>`.
- insert:
    - id: codex-static
      name: '@dsh-external/dsh-plugin-codex-ui'
